# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Insta' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Insta
  pod 'Appirater'
  
  # Firebase
  pod 'Firebase/Core', '~> 10.1.0'
  pod 'Firebase/Auth', '~> 10.1.0'
  pod 'Firebase/Database', '~> 10.1.0'
  pod 'Firebase/Analytics', '~> 10.1.0'
  pod 'Firebase/Crashlytics', '~> 10.1.0'

  #pod 'Firebase/Storage', '~> 10.1.0'
  #pod 'Firebase/AppCheckInterop', '~> 10.1.0'
  #pod 'Firebase/AuthInterop', '~> 10.1.0'
  #pod 'Firebase/CoreExtension', '~> 10.1.0'

  target 'InstaTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'InstaUITests' do
    # Pods for testing
  end

end
